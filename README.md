# Ruby Todo CLI ✅

A simple command-line todo list application written in Ruby.  
This project demonstrates basic Ruby functionality and uses a YAML config file.

## 🚀 Features
- Add tasks
- List tasks
- Mark tasks as done
- Configurable settings via `config.yml`

## 📦 Run
```bash
ruby main.rb
